# Copia questo file in .env e personalizza i valori
# Genera un nuovo CLUSTER_ID con:
#   python3 -c "import uuid,base64; print(base64.urlsafe_b64encode(uuid.uuid4().bytes).rstrip(b'=').decode())"

CLUSTER_ID=<genera-con-comando-sopra>
PRODUCER_DELAY_MS=500
ES_HEAP_SIZE=512m
MONGO_INITDB_DATABASE=sensor_data
